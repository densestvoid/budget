#cloud-config
package_update: true
package_upgrade: true

packages:
  - docker.io
  - docker-compose
  - curl
  - wget
  - nginx
  - postgresql-client
  - at  # For scheduling auto-termination

# Create budget user
users:
  - name: budget
    groups: docker
    sudo: ALL=(ALL) NOPASSWD:ALL
    shell: /bin/bash
    home: /home/budget

# Write environment file
write_files:
  - path: /home/budget/.env
    content: |
      DATABASE_URL=${database_url}
      PORT=${app_port}
      ENV=production
      LOG_LEVEL=info
      GITHUB_REPO=${github_repo}
      GITHUB_BRANCH=${github_branch}
      AUTO_TERMINATE_MINUTES=${auto_terminate_minutes}
    owner: budget:budget
    permissions: '0600'
  
  - path: /home/budget/docker-compose.yml
    content: |
      version: '3.8'
      services:
        postgres:
          image: postgres:16-alpine
          container_name: budget-postgres
          restart: unless-stopped
          environment:
            POSTGRES_DB: budget
            POSTGRES_USER: budget_user
            POSTGRES_PASSWORD: budget_password
            POSTGRES_INITDB_ARGS: "--encoding=UTF8 --lc-collate=en_US.UTF-8 --lc-ctype=en_US.UTF-8"
          volumes:
            - postgres_data:/var/lib/postgresql/data
          networks:
            - budget-network
          healthcheck:
            test: ["CMD-SHELL", "pg_isready -U budget_user -d budget"]
            interval: 10s
            timeout: 5s
            retries: 5
            start_period: 10s

        app:
          image: ${docker_image_url}
          container_name: budget-app
          restart: unless-stopped
          ports:
            - "${app_port}:${app_port}"
          env_file:
            - .env
          depends_on:
            postgres:
              condition: service_healthy
          networks:
            - budget-network
          healthcheck:
            test: ["CMD", "curl", "-f", "http://localhost:${app_port}/health"]
            interval: 30s
            timeout: 10s
            retries: 3
            start_period: 40s

        nginx:
          image: nginx:alpine
          container_name: budget-nginx
          restart: unless-stopped
          ports:
            - "80:80"
            - "443:443"
          volumes:
            - ./nginx.conf:/etc/nginx/nginx.conf:ro
          depends_on:
            - app
          networks:
            - budget-network

      networks:
        budget-network:
          driver: bridge

      volumes:
        postgres_data:
          driver: local
    owner: budget:budget
    permissions: '0644'

  - path: /home/budget/nginx.conf
    content: |
      events {
          worker_connections 1024;
      }

      http {
          upstream app {
              server app:${app_port};
          }

          server {
              listen 80;
              server_name _;

              # Health check endpoint
              location /health {
                  proxy_pass http://app;
                  proxy_set_header Host $host;
                  proxy_set_header X-Real-IP $remote_addr;
                  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                  proxy_set_header X-Forwarded-Proto $scheme;
              }

              # Main application
              location / {
                  proxy_pass http://app;
                  proxy_set_header Host $host;
                  proxy_set_header X-Real-IP $remote_addr;
                  proxy_set_header X-Real-Forwarded-For $proxy_add_x_forwarded_for;
                  proxy_set_header X-Forwarded-Proto $scheme;
                  proxy_connect_timeout 30s;
                  proxy_send_timeout 30s;
                  proxy_read_timeout 30s;
              }
          }
      }
    owner: budget:budget
    permissions: '0644'

  - path: /home/budget/deploy.sh
    content: |
      #!/bin/bash
      set -e
      
      echo "$(date): Starting pre-built image deployment..." | tee -a /var/log/deployment.log

      cd /home/budget
      
      # Login to GitHub Container Registry
      echo "$(date): Logging into GitHub Container Registry..." | tee -a /var/log/deployment.log
      echo "${github_token}" | docker login ghcr.io -u github --password-stdin
      
      # Pull the pre-built image
      echo "$(date): Pulling Docker image ${docker_image_url}..." | tee -a /var/log/deployment.log
      if ! docker pull ${docker_image_url}; then
        echo "$(date): Failed to pull Docker image" | tee -a /var/log/deployment.log
        exit 1
      fi
      
      # Start containers
      echo "$(date): Starting containers..." | tee -a /var/log/deployment.log
      docker-compose down 2>/dev/null || true
      docker-compose up -d
      
      # Wait for PostgreSQL
      echo "$(date): Waiting for PostgreSQL..." | tee -a /var/log/deployment.log
      timeout 120 bash -c 'until docker-compose exec -T postgres pg_isready -U budget_user -d budget; do sleep 2; done'
      
      # Wait for application
      echo "$(date): Waiting for application..." | tee -a /var/log/deployment.log
      timeout 120 bash -c 'until curl -f http://localhost:${app_port}/health; do sleep 2; done'
      
      # Run migrations
      echo "$(date): Running database migrations..." | tee -a /var/log/deployment.log
      docker-compose exec -T app ./budget migrate 2>&1 | tee -a /var/log/deployment.log || echo "Migration command not available"
      
      echo "$(date): Deployment completed successfully!" | tee -a /var/log/deployment.log
    owner: budget:budget
    permissions: '0755'

  - path: /home/budget/auto-terminate.sh
    content: |
      #!/bin/bash
      echo "Auto-termination triggered after ${auto_terminate_minutes} minutes"
      echo "Deployment info: Repo=${github_repo}, Branch=${github_branch}"
      
      # Stop the application gracefully
      cd /home/budget
      docker-compose down
      
      # Log the termination
      echo "$(date): Auto-termination executed" >> /var/log/auto-terminate.log
      
      # Shutdown the system
      shutdown -h now
    owner: budget:budget
    permissions: '0755'

  - path: /etc/systemd/system/budget-app.service
    content: |
      [Unit]
      Description=Budget Application
      Requires=docker.service
      After=docker.service

      [Service]
      Type=oneshot
      RemainAfterExit=yes
      WorkingDirectory=/home/budget
      ExecStart=/home/budget/deploy.sh
      TimeoutStartSec=300
      User=budget
      Group=budget

      [Install]
      WantedBy=multi-user.target
    permissions: '0644'

runcmd:
  - systemctl enable docker
  - systemctl start docker
  - usermod -aG docker budget
  - chown -R budget:budget /home/budget
  
  - systemctl daemon-reload
  - systemctl enable budget-app.service
  - systemctl start budget-app.service
  
  # Schedule auto-termination
  - systemctl enable atd
  - systemctl start atd
  - |
    echo "/home/budget/auto-terminate.sh" | at now + ${auto_terminate_minutes} minutes
    echo "Auto-termination scheduled for ${auto_terminate_minutes} minutes from now"

final_message: "Budget app server setup complete. Pre-built image deployment started. Auto-termination scheduled for ${auto_terminate_minutes} minutes."